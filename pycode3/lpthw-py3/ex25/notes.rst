``list`` has method ``pop()`` which removes the element of list and returns it.

::

    L.pop([index]) -> item -- remove and return item at index (default last).
    Raises IndexError if list is empty or index is out of range.